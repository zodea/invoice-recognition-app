/diagonse 每完成一项做对应的commit全部完成后做push
1、当没有文件上传的时候，顶部也同时需要展示右侧的勾选、价税合计等内容
2、导出文件是需要将所有文件放进一个文件夹内，而不是现在的分好几个文件导出
3、分目录这个需要做个复选下拉框，按购买方、销售方、日期，发票类型（专票、普票）建文件夹分类放置。如果同时有选择，先先后顺序依次列文件夹，并在打印排版和识别内容中同步进行该步骤，方便体现区别。
4、打印按钮点击后出现的是文字按Ctrl+P打印，但实际需要打印的部分是打印排版，出来的东西不对
5、如果还没有做识别，那PDF和EXCEL这两个按钮就不能点击，并且还是要和整理导出一样，做一个选择目录保存
6、有多少张发票那行，然后还需要加一个去重
7、购买方和类型那里在未识别的时候先留空
8、在识别内容那里需要添加一个整理导出后的发票名称显示，这个作为一个小字在原文件名下面展示
9、完善后将界面的滚动条尽可能的隐藏，现在效果比较难看

---

## 完成情况（branch: feat/invoice-polish，逐项 commit）

- [x] 1 空状态也显示顶部右侧 勾选/价税合计 面板（InvoicePrintPanel 去掉 v-if）
- [x] 2 整理导出统一收进一个父文件夹「发票整理_最早至最晚」
- [x] 3 分目录改多选下拉：购买方/销售方/日期/发票类型(专票普票)，勾选先后即嵌套层级；
      左右两栏按同维度分组排序、行内显示 📁 分组路径
- [x] 4 打印按钮直接打印「打印排版 PDF」（隐藏 iframe + contentWindow.print，回退新标签）
- [x] 5 未识别时禁用 PDF/Excel；PDF/Excel 改为选目录保存（Tauri/FSA，回退下载）
- [x] 6 张数行加「去重」按钮 + 「含 N 张重复(已排除)」徽标
- [x] 7 购买方/类型未识别时留空（筛选桶=未识别，输入框占位「未识别（留空）」）
- [x] 8 识别内容每行原文件名下方小字显示整理后文件名（销售方：日期=金额元.pdf）
- [x] 9 全局隐藏滚动条（细、透明轨道、悬停显淡灰滑块）
- [x] 识别提速：预览光栅化 scale 自适应(2→1.0~1.6)、批量去重只在结束跑一次
- [x] Tauri：cargo check 通过；npm run tauri:build 出便携桌面应用