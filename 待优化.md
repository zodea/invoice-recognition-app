/diagonse 
1、将这个上传至github，新建一个发票识别的仓库，并将下面的功能各做一个commit分开提交
2、中文界面需要，并且有功能重合的按钮，需要清除。下面两个框框的打印排版、识别内容需要固定，不能随着拖动就走了。排版看一下再优化一下，头部需要清凉一点，现在太多功能冗余和占位了
3、识别的文件还是有很多发票号码、销售方、购买方、金额、税额没识别出来。
4、识别的文件需要按购买方分开，需要添加几个按钮做购买方切换。然后对于不同的pdf类型需要分开，比如发票、货运电子收款凭证等
5、备注应该显示文件中的备注，如果是系统执行有其他状况的备注需要另外展示.
6、目前左侧的发票预览显示速度有点慢，需要优化一下，按插入的顺序分批加载。
7、导出的文件保存目录要么设置一个默认路径，要么给我一个弹窗选择，目前不知道保存到哪里了。导出时需要加一个可选按钮，分购买方的目录放置，可以将输入的文件全部更名，并按销售方:yyyy-mm-dd=money.00元的格式导出。统计的excel文件放在目录的最外层